import React from 'react';

const styles = {
    allCenter : `flex justify-center items-center`,
}

const Vote = () => {

  return (
    <>
        <section className={`${styles.allCenter}`}>
            <h1>Coming Soon</h1>
        </section>
    </>
    )
}

export default Vote